name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: ${{ hashFiles('pnpm-lock.yaml') != '' && 'pnpm' || '' }}

      - name: Enable Corepack
        run: corepack enable

      - name: Validate cloud boundary
        run: node scripts/validate-cloud-boundary.mjs

      - name: Validate patch whitespace
        run: git diff --check HEAD^ HEAD

      - name: Require a lockfile when application code exists
        shell: bash
        run: |
          if [[ -f package.json && ! -f pnpm-lock.yaml ]]; then
            echo "package.json exists without pnpm-lock.yaml"
            exit 1
          fi

      - name: Install locked dependencies
        if: ${{ hashFiles('pnpm-lock.yaml') != '' }}
        run: pnpm install --frozen-lockfile

      - name: Run repository quality scripts
        if: ${{ hashFiles('package.json') != '' }}
        shell: bash
        run: |
          for script in lint typecheck test build; do
            if node -e "const p=require('./package.json'); process.exit(p.scripts?.['$script'] ? 0 : 1)"; then
              pnpm "$script"
            fi
          done
